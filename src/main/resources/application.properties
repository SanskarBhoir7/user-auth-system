spring.application.name=user-auth-system

# ===============================
# DATABASE CONFIGURATION
# ===============================
# Use environment variables for security
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/userdb}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=Sb#031106

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===============================
# JWT CONFIGURATION
# ===============================
# IMPORTANT: Set JWT_SECRET as environment variable in production!
# Generate with: openssl rand -base64 32
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=${JWT_EXPIRATION:3600000}

# ===============================
# EMAIL CONFIGURATION
# ===============================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=bhoirsanskar711@gmail.com
spring.mail.password=bncbcmttbsixplgl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Application URL for email links
app.url=${APP_URL:http://localhost:8080}
